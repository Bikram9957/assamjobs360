# TODO

## Style Fix (CSS not applying)
- [x] Build the stylesheet URL from the request-derived installation path.
- [x] Remove the hardcoded `BASE_URL` dependency from public navigation and admin redirects.
- [x] Build the canonical URL from the request path without concatenating it to a base URL.
- [x] Validate the changed PHP files with the XAMPP PHP linter.

